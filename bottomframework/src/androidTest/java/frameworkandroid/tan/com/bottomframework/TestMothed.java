package frameworkandroid.tan.com.bottomframework;

import android.test.AndroidTestCase;


public class TestMothed extends AndroidTestCase{

  public void testGetTaskInfos()throws Exception{

     /* RiZhiDB riZhiDB=new RiZhiDB(getContext());
      Work work=new Work();
      riZhiDB.insertAlarm(work);*/
   /* Utils.init(getContext());
    int width=ScreenUtils.getScreenWidth();
    int height=ScreenUtils.getScreenHeight();
    MyLog.ShowLog("屏幕宽 "+width+"像素 屏幕高 "+height+"像素");
    MyLog.ShowLog("屏幕宽 "+ DensityUtil.px2dip(getContext(), width)+"dp 屏幕高 "+DensityUtil.px2dip(getContext(),height)+"dp");
//    计算1像素等于多少dp   1dp相当于2个像素  红米1s手机
    MyLog.ShowLog("10sp相当于的像素数 "+ DensityUtil.sp2px(getContext(), 10)+"px");
*/

   /* String jsonData= AssetsUtils.getAssetsData(mContext, "FisheriesProduct/user/news_detail1.txt");
    Type type = new TypeToken<NewsDetailModel>() {
    }.getType();
    NewsDetailModel newsDetailModel = (NewsDetailModel) JsonUtil.convertJsonToObject1(jsonData, type);
    MyLog.ShowLog(newsDetailModel.toString());*/

    // json模拟数据 转化成对象
    /*String json = AssetsUtils.getAssetsData(mContext, "FisheriesProduct/user/moban.txt");
    List<SystemList> entity= (List<SystemList>) JsonUtil.convertJsonToObject1(json);
    MyLog.ShowLog(entity.toString());*/

  }
}
